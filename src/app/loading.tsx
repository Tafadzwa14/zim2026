export default function Loading() {
  return (
    <main className="mx-auto flex min-h-dvh max-w-lg items-center justify-center px-6" aria-busy="true">
      <p className="mono text-sm font-semibold text-muted">Loading Zim 2026…</p>
    </main>
  );
}
